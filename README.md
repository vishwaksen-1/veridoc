# veridoc

CI-native documentation layer for RTL projects.

* Automatically generates and maintains documentation from Verilog source files.
* Keeps docs always in sync with RTL (no manual drift).
* Designed for real engineering workflows, not academic parsing.
